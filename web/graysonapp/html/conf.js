var graysonConf = {
	clientId           : "{{ clientId }}",
	socketioListenPort : "{{ app.socketioListenPort }}",
	uriPrefix          : "{{ app.URL_PREFIX }}",
	staticDataURL      : "{{ STATIC_URL }}",
	unitTest           : false
    };