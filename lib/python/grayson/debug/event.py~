from grayson.net.amqp import GraysonAMQPTransmitter

class EventStream (object):

    def __init__(self, amqpPort):
        self.amqpPort = amqpPort

    def publish (self):

    def sendJobStatusEvent (self, username, workflowId, jobid, status):
        self.publish ({
                "clientId"   : self.username, 
                "workflowId" : outputWorkflowPath,
                "event"      : {
                    "type"  : "jobstatus",
                    "job"   : jobid,
                    "state" : status
                    }
                },
